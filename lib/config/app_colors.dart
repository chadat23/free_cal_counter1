import 'package:flutter/material.dart';

class AppColors {
  static const Color largeWidgetBackground = Color(0xFF1C1C1C);
  static const Color smallWidgetBackground = Color(0xFF2D2D2D);

  // Search result background colors by data quality (Temporary Debug Colors)
  static const Color searchResultGood = Color.fromARGB(255, 43, 0, 0);
  static const Color searchResultBetter = Color.fromARGB(255, 0, 0, 26);
  static const Color searchResultBest = Color.fromARGB(255, 0, 31, 0);
}
